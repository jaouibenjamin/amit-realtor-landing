<div dir="rtl">

# סטטוס אמיתי — מה מוכן ומה עוד נשאר

> **חשוב ליישור ציפיות:** האתר נמצא בשלב **front-end POC** — לא קרוב ל-launch.
> מה שמוכן הוא הצד הויזואלי + תוכן דמה. נשארה עבודת **backend / תשתית / QA** משמעותית.

---

## ✅ מה מוכן (Front-end בלבד)
- עיצוב מלא, RTL, מונוכרום כהה (navy) + לוגו.
- 3 שפות (HE/EN/FR) — **התרגום עדיין לא מוגה ע״י native**.
- Hero עם תמונת רקע ו-scroll-blur, רשת נכסים + סינון, כרטיסי "נמכרו", סקשן "על עמית".
- טופס לידים ש**מרכיב הודעת וואטסאפ** (בלי שמירה).
- GA4 מוטמע אך **חסום עד הסכמה ולא פעיל** (אין עדיין Measurement ID).
- פריסה ל-GitHub Pages (סטטי).

---

## 🔧 מה עוד נשאר — וזה לא מעט

### Backend / נתונים (העבודה האמיתית)
- **צנרת לידים אמיתית** — היום הטופס רק פותח וואטסאפ. אין שמירת לידים, אין DB, אין CRM, אין anti-spam, אין מעקב/סטטוס. צריך שירות (Formspree / Make / Google Sheets) או backend ממשי.
- **ניהול נכסים** — כל נכס **מקודד ידנית ב-HTML**. הוספה/עדכון = שינוי קוד. אם הנכסים מתחלפים תכופות צריך CMS / headless / feed (Yad2/Madlan).
- אין שרת ואין DB — האתר 100% סטטי.

### Production / הקשחת Front-end
- **Tailwind דרך CDN** (גרסת play — לא לפרודקשן) → לקמפל ל-CSS סטטי, self-host, ולהוסיף SRI.
- אופטימיזציית תמונות אמיתיות (WebP, lazy-load, גדלים נכונים).
- ביצועים (Lighthouse), נגישות (a11y), SEO (meta/OG לכל שפה, sitemap, favicon), Google Business Profile.

### תשתית
- דומיין + DNS + SSL; החלטת hosting; אולי הגנת גישה בזמן בנייה (noindex / סיסמה).

### אנליטיקס / פרטיות
- הקמת חשבון GA4 + Measurement ID + Consent Mode מלא.
- דף **מדיניות פרטיות**; עמידה בחוק הגנת הפרטיות (ישראל) ו-GDPR.

### אבטחה (מסקירת ה-Security Board)
- החלפת PAT קלאסי ב-fine-grained, הפעלת 2FA, secret-scanning, Enforce HTTPS.

---

## הערכת שלבים
- **שלב A — launch בסיסי:** תוכן אמיתי + תמונות + דומיין + production build + טופס לידים אמיתי (Formspree) + GA4 + פרטיות + QA.
- **שלב B — הרחבה:** ניהול נכסים (CMS), אתר רב-עמודים, FAQ, "אקדמיה"/תוכן, אוטומציות.

**שורה תחתונה:** מה שמוכן זה ~30% מהדרך ל-launch — החלק הויזואלי. ה-70% שנותרו הם תוכן + backend + תשתית + QA.

</div>
